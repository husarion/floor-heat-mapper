# Quick Start (at fist stop ROS Master if working on host)
# $ docker compose -f compose.rosbot.heatmapping.yaml up
version: "3"
services:
  micro-ros-agent:
    image: microros/micro-ros-agent:humble
    volumes:
      - /dev:/dev
    devices:
      - /dev/ttyACM0:/dev/ttyACM0
    command: serial --dev /dev/ttyACM0 -v6


